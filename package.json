{
  "name": "dreamcoder-dsh",
  "version": "0.2.0",
  "private": true,
  "description": "Dreamcoder engineering operating layer (Gentle-AI philosophy) as a DeepSeek Harness out-of-tree bundle + profile",
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "install:dsh": "bash scripts/install.sh",
    "typecheck": "tsgo --noEmit",
    "test": "node --test 'scripts/*.test.ts'",
    "verify": "node scripts/verify-compat.ts && node scripts/verify-presets.ts && node scripts/verify-contracts.ts",
    "bench": "node scripts/dream-bench.ts"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "@typescript/native-preview": "^7.0.0-dev.20260707.2"
  }
}
